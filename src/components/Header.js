import React from "react";
import "../style.css";

export default class Header extends React.Component {
  render() {
    return (
      <nav>
        <p className="header--title">CV Creator</p>
      </nav>
    );
  }
}
